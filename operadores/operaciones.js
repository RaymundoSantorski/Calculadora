const { sumar } = require('./suma');
const { restar } = require('./resta');
const { multiplicar } = require('./multiplicacion');
const { dividir } = require('./division');
const { elevar } = require('./potencia');

module.exports = {
    sumar,
    restar,
    multiplicar,
    dividir,
    elevar
}